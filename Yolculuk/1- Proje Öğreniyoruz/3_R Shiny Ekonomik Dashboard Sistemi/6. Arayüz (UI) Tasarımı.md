- [ ] R dosyasını aç
- [ ] **`dashboardSidebar()`** kısmını bul
- [ ] **`width parametresini`** ekle

```r
dashboardSidebar(
  width = 260,
  sidebarMenu(

    menuItem("Yönetici Özeti", tabName = "yonetici", icon = icon("chart-line")),
    menuItem("Tahmin Laboratuvarı", tabName = "laboratuvar", icon = icon("flask")),
    menuItem("Model Röntgeni", tabName = "model", icon = icon("microscope"))

  )
),
```

- [ ] Sidebar içine en üste logo alanı ekle

### 💻 Kod

```r
tags$div(
  style = "
    text-align:center;
    padding:20px;
    color:white;
    font-size:22px;
    font-weight:bold;
  ",
  "MacroVision TR"
),
```

- [ ] **`dashboardHeader(`**) içine ekle

```r
tags$li(
  class = "dropdown",
  tags$span(
    style = "
      color:#00ff66;
      font-weight:bold;
      padding:15px;
    ",
    "● Sistem Durumu: Çevrimiçi"
  )
),
```

- [ ] Header içine saat alanı ekle

```r
output$saat <- renderText({
  paste("Son Güncelleme:", format(Sys.time(), "%H:%M:%S"))
})
```

```r
tags$li(  class = "dropdown",  textOutput("saat"))
```

- [ ] Header içine dark buton ekle

```r
tags$li(
  class = "dropdown",
  actionButton(
    "tema",
    "Dark Mode"
  )
),
```

- [ ] **`dashboardBody()`** içine ekle

```r
tags$head(
  tags$style(HTML("
    .main-sidebar { background-color: #111827 !important; }
    .main-header .logo { background-color: #0f172a !important; color: white !important; }
    .main-header .navbar { background-color: #0f172a !important; }
    .sidebar-menu li:hover { background-color: #1e293b !important; }
  "))
),
```

- [ ] Server kısmına ekle

```r
observeEvent(input$tema, {
  session$sendCustomMessage("toggleDark", list())
})
```

```r
tags$script(HTML("
  Shiny.addCustomMessageHandler('toggleDark', function(msg) {
    document.body.classList.toggle('dark-mode');
  });
"))
```